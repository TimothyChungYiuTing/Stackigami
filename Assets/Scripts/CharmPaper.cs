using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharmPaper : Card
{
    // Start is called before the first frame update
    public int attack = 0;
    public float attackCD = 1;
    public int health = 0;
    public int attr = 0;

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
